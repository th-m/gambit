## Codebase Patterns

- **Utils location**: Game-specific utility functions go in `app/utils/` (e.g., characterAssignment.ts)
- **Services location**: Business logic services go in `app/services/` with class + singleton pattern (same as registries)
- **TypeScript types location**: Core game types go in `app/types/game.ts`, Supabase generated types in `app/types/supabase.ts`
- **Validation command**: Use `npm run validate` to run both typecheck and tests
- **Type unions over enums**: Use string literal union types (e.g., `type GamePhase = 'lobby' | ...`) rather than TypeScript enums for better tree-shaking and JSON compatibility
- **Constants alongside types**: Export const arrays (e.g., `GAME_PHASES`) alongside type unions for runtime validation and iteration
- **Testing framework**: Vitest configured in `vite.config.ts`, tests go in `app/**/*.test.ts`
- **API routes location**: React Router file-based routes in `app/routes/`, use `api.*.tsx` naming for API endpoints (e.g., `api.games.create.tsx` → `/api/games/create`)
- **API route pattern**: Export `action` function for POST/PUT/DELETE, use `loader` for GET. Return `new Response(JSON.stringify(data), { status, headers })` for JSON responses
- **Type guard pattern**: Use `isX(value: unknown): value is X` pattern with const arrays for validation
- **Registry pattern**: Class-based registries in `app/registry/` with Map storage, export both class (for testing) and singleton instance (for production)
- **React contexts location**: Context providers go in `app/contexts/` - export Provider component, useX hook, and optionally the Context for testing
- **React component tests**: Use `@vitest-environment jsdom` annotation at file top; React Router plugin is disabled during tests via vite config

---
## 2026-01-25 - types-game-core
- **What was implemented**: Core TypeScript type definitions for all game entities
- **Files changed**: 
  - `app/types/game.ts` (created)
- **Learnings:**
  - Existing Supabase types in `app/types/supabase.ts` use a different schema than the technical plan; new types need to align with the technical plan's database schema
  - Use `Record<string, unknown>` for JSON metadata fields to allow flexibility while maintaining type safety
  - Include helper types like `NewGame`, `GameUpdate` for common operations

---
## 2026-01-25 - types-actions-effects
- **What was implemented**: TypeScript types for the action and effect registry system
- **Files changed**: 
  - `app/types/game.ts` (updated - added action/effect types)
- **Learnings:**
  - Function types (`ValidateTargetsFn`, `ExecuteActionFn`, `ResolveInfoFn`, `EventHookFn`) are cleaner as type aliases rather than inline signatures
  - `ActionDefinition` includes both sync and async execute via `ActionResult | Promise<ActionResult>`
  - Effect modifiers focus on perception changes (`appears_as_seer`, `appears_as_good`) which are core to the game's deduction mechanics
  - `GameContext` consolidates all state needed for registry operations (game, players, currentPlayer, modifiers, statuses)

---
## 2026-01-25 - test-types-validation
- **What was implemented**: Type guard functions and unit tests for all type definitions
- **Files changed**: 
  - `app/types/game.ts` (updated - added type guards and constants)
  - `app/types/game.test.ts` (created - 37 unit tests)
  - `package.json` (added test and validate scripts)
  - `vite.config.ts` (added Vitest configuration)
- **Learnings:**
  - Vitest integrates seamlessly with existing Vite config - just add `test` property
  - Type guards use `(CONST_ARRAY as readonly string[]).includes(value)` pattern to avoid TypeScript narrowing issues
  - Tests should cover: valid values from const arrays, invalid strings, case sensitivity, null, undefined, and wrong primitive types (number, boolean, object, array)
  - Added `ACTION_TYPES`, `LEADER_VOTES`, `MISSION_VOTES` const arrays that were missing for runtime validation

---
## 2026-01-25 - registry-action
- **What was implemented**: ActionRegistry class for managing character action definitions
- **Files changed**: 
  - `app/registry/ActionRegistry.ts` (created)
- **Learnings:**
  - Registry folder created at `app/registry/` for all registry classes
  - Class-based registry allows both singleton pattern (exported instance) and injectable instances (for testing)
  - `getAvailableActions` filters by: current phase, character's action list, and use limits
  - `checkConditions` validates: phase, use limits, and team requirements before execution
  - `execute` is async to support both sync and async action handlers
  - Helper functions `countActionUses` and `getUsedActionIds` extract usage data from GameAction[] array
  - Target validation is two-step: count check first, then action-specific validateTargets function

---
## 2026-01-25 - test-registry-action
- **What was implemented**: Comprehensive unit tests for ActionRegistry class
- **Files changed**: 
  - `app/registry/ActionRegistry.test.ts` (created - 41 unit tests)
- **Learnings:**
  - Create helper factory functions (`createTestGame`, `createTestPlayer`, `createTestContext`, `createTestAction`) for cleaner test setup
  - Use `vi.fn()` from Vitest for mocking function calls and verifying invocations
  - Test both success and failure paths for execute() - validate conditions, target counts, target validation, and async handling
  - When testing with ActionId arrays, explicitly type them as `ActionId[]` to avoid TypeScript inference issues with string literals
  - Test registry's `clear()` method for resetting state between test scenarios
  - Helper functions (`countActionUses`, `getUsedActionIds`) should be tested separately from the main registry class

---
## 2026-01-25 - registry-effect
- **What was implemented**: EffectRegistry class for managing passive character effects
- **Files changed**: 
  - `app/registry/EffectRegistry.ts` (created)
- **Learnings:**
  - EffectRegistry is simpler than ActionRegistry - effects are passive, not executed like actions
  - `getModifiers` and `getAllModifiers` provide access to perception-changing effects (appears_as_seer, appears_as_good)
  - `hasModifier` helper simplifies checking if a perception modifier is active
  - `triggerHooks` handles both sync and async hook handlers via Promise.all pattern
  - No condition checking needed for effects - they're always active when the character has them
  - `getEffectsWithHook` useful for determining which effects respond to specific game events

---
## 2026-01-25 - test-registry-effect
- **What was implemented**: Comprehensive unit tests for EffectRegistry class (41 tests)
- **Files changed**: 
  - `app/registry/EffectRegistry.test.ts` (created - 41 unit tests)
- **Learnings:**
  - Reuse helper factory functions from ActionRegistry tests (`createTestGame`, `createTestPlayer`, `createTestContext`)
  - Create `createTestEffect` helper for consistent EffectDefinition mocking
  - Test async hooks with `mockImplementation(async () => ...)` and verify completion with Promise timing
  - Perception effect tests should verify: modifier presence, hasModifier queries, and absence of cross-effect contamination
  - Test that `appears_as_seer` does NOT affect Seer perception (only Oracle)
  - Test that `appears_as_good` does NOT affect other evil players' knowledge (only Seer)
  - Integration tests helpful for showing how modifiers are queried during info resolution

---
## 2026-01-25 - registry-character
- **What was implemented**: CharacterRegistry class with all 10 character definitions and info resolution
- **Files changed**: 
  - `app/registry/CharacterRegistry.ts` (created)
- **Learnings:**
  - CharacterRegistry follows same singleton + class pattern as other registries
  - `resolveInfo(ctx)` delegates to each character's `info` function which takes GameContext and returns CharacterInfo
  - Info resolution uses EffectRegistry to check for perception modifiers:
    - `getVisibleEvilPlayers` excludes players with `appears_as_good` effect (Saboteur hidden from Seer)
    - `getSeerCandidates` includes real Seer + players with `appears_as_seer` effect (Phantom confuses Oracle)
  - `buildPlayerLabels` helper creates label mappings for UI display
  - Evil team characters all share similar info logic (knowing other evil players)
  - Character definitions include: name, team, description, info resolver, actions array, effects array
  - Export `registerAllCharacters()` function for app initialization
  - Export individual character definitions for unit testing

---
## 2026-01-25 - test-registry-character
- **What was implemented**: Comprehensive unit tests for CharacterRegistry class (39 tests)
- **Files changed**: 
  - `app/registry/CharacterRegistry.test.ts` (created - 39 unit tests)
- **Learnings:**
  - Tests require registering both characters AND effects; `registerAllCharacters()` populates characterRegistry, but `registerPerceptionEffects()` helper needed for effectRegistry
  - Clear BOTH singleton registries in beforeEach/afterEach to prevent test pollution
  - Test info resolution requires building a realistic player context with multiple players of different teams
  - Oracle edge cases: when only Seer present, label is "Seer"; when Phantom also present, labels become "Seer?" to indicate uncertainty
  - Test all evil characters share the same info pattern (knowing other evil players) - parameterized test verifies this efficiently
  - Export individual character definitions (seerDefinition, etc.) enables testing character properties directly without needing to look them up from registry

---
## 2026-01-25 - registry-eventbus
- **What was implemented**: EventBus class for game event pub/sub system
- **Files changed**: 
  - `app/registry/EventBus.ts` (created)
- **Learnings:**
  - EventBus uses `Set<EventHandler>` per event type for automatic deduplication of handlers
  - `on()` returns an unsubscribe function (closure pattern) for easy cleanup
  - Middleware pattern uses chained async functions built from end to beginning
  - `emit()` supports both sync and async handlers via Promise.all
  - Export additional types (`EventHandler`, `Unsubscribe`, `EventMiddleware`) for consumers
  - Helper methods `getHandlerCount()` and `getActiveEvents()` useful for testing and debugging
  - `off(event)` removes all handlers for an event; `clear()` removes all handlers and middleware

---
## 2026-01-25 - test-registry-eventbus
- **What was implemented**: Comprehensive unit tests for EventBus class (48 tests)
- **Files changed**: 
  - `app/registry/EventBus.test.ts` (created - 48 unit tests)
- **Learnings:**
  - Test middleware chain execution order with call order arrays - middleware wraps in onion pattern (m1-start, m2-start, handler, m2-end, m1-end)
  - Test middleware blocking by not calling `next()` - handler should not be invoked
  - Test middleware data modification by mutating the eventData object before calling `next()`
  - Use `it.each()` from Vitest to test all 13 GameEventType values systematically
  - Test idempotency of unsubscribe function (calling multiple times should not error)
  - Test both singleton instance (`eventBus`) and class constructor (`new EventBus()`) for isolation
  - `clearMiddleware()` useful for testing handler behavior without middleware interference

---
## 2026-01-25 - service-game-crud
- **What was implemented**: GameService class for game and player CRUD operations
- **Files changed**: 
  - `app/services/GameService.ts` (created)
- **Learnings:**
  - Services go in `app/services/` directory, following same pattern as registries
  - In-memory Map storage works well for development; can be swapped for Supabase queries later
  - `generateGameKey()` uses uppercase alphanumeric chars excluding ambiguous ones (I, O, 0, 1) for readability
  - Game key index (Map<gameKey, gameId>) enables O(1) lookup by shareable code
  - `addPlayer` handles duplicate joins gracefully by returning existing player instead of throwing
  - Players sorted by `seat_order` (null values sorted to end) for consistent ordering
  - UUID generation can be inline until production needs crypto.randomUUID()
  - Export both class (for testing with fresh instances) and singleton (for production)

---
## 2026-01-25 - test-service-game
- **What was implemented**: Comprehensive unit tests for GameService class (47 tests)
- **Files changed**: 
  - `app/services/GameService.test.ts` (created - 47 unit tests)
- **Learnings:**
  - Test `generateGameKey` separately from service class - it's an exported utility
  - Use regex patterns to validate UUID format: `/^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i`
  - Test game key case-insensitivity (service should handle lowercase input)
  - Test error paths with `expect(() => ...).toThrow('message')` pattern
  - `addPlayer` graceful duplicate handling is important - returns existing player rather than throwing
  - Test seat_order sorting with explicit order assignment to verify sort behavior
  - Test game key index update when `game_key` changes in updateGame
  - Service tests don't need helper factory functions like registry tests - work directly with service methods

---
## 2026-01-25 - service-vote-processor
- **What was implemented**: VoteProcessor service for handling leader and mission vote submission and resolution
- **Files changed**: 
  - `app/services/VoteProcessor.ts` (created)
- **Learnings:**
  - VoteProcessor maintains its own storage for GameAction, GameModifier, and PlayerStatus entities (separate from GameService)
  - GameService dependency is injected via constructor for testability - defaults to singleton but accepts custom instance
  - Vote recording uses action_type 'vote_yes'/'vote_no' for leader votes, 'vote_pass'/'vote_fail' for mission votes
  - Mission size lookup table (MISSION_SIZES) is keyed by player count then round number for O(1) lookup
  - Round 4 with 7+ players requires 2 fail votes - extracted to `getRequiredFailVotes` helper
  - 3-rejection auto-fail awards evil a victory and advances the round (not just the crown)
  - Good winning 3 missions triggers assassination phase if Assassin is alive, otherwise immediate good victory
  - Modifiers like `force_pass` and `extra_fail` are checked during mission result processing
  - Beeper vibration is triggered before state update to ensure statuses are still valid
  - `cleanupRound` removes expired statuses (by expires_at_round) and modifiers (by round number)

---
## 2026-01-25 - test-service-vote-processor
- **What was implemented**: Comprehensive unit tests for VoteProcessor class (48 tests)
- **Files changed**: 
  - `app/services/VoteProcessor.test.ts` (created - 48 unit tests)
- **Learnings:**
  - GameService's `addPlayer` checks `status !== 'lobby'` - must add players BEFORE updating game status to 'playing'
  - Test helper functions should create players first, then update game status/phase
  - When testing character-dependent scenarios (like "Assassin is dead"), remember setup may create MULTIPLE players with the same character - use `filter` instead of `find` to handle all instances
  - The `getMissionSize` and `getRequiredFailVotes` helper functions are exported and should be tested independently
  - Test both modifiers (`force_pass`, `extra_fail`) and their effects on mission outcomes
  - Leader vote approval requires strict majority (ties go to rejection)
  - Verify the full chain: vote submission → tally → result processing → state update

---
## 2026-01-25 - service-action-processor
- **What was implemented**: ActionProcessor service for executing character special abilities
- **Files changed**: 
  - `app/services/ActionProcessor.ts` (created)
- **Learnings:**
  - ActionProcessor delegates core validation to ActionRegistry (phase, use limits, targets) - keeps single responsibility
  - Inject dependencies (GameService, VoteProcessor) via constructor for testability; use singleton defaults for production
  - Handler registration pattern (`registerHandler(actionId, handler)`) allows action-specific side effects (e.g., eliminate player, add modifier) to be defined separately from validation
  - `buildContext()` helper creates GameContext by aggregating game state, players, modifiers, and statuses
  - `getAllStatuses()` collects player statuses across all players in game - returns flat array, not array of arrays
  - Record action AFTER successful execution via registry, not before
  - Export helper methods (eliminatePlayer, addModifier, addStatus, endGame) for action handlers to use
  - ActionProcessor orchestrates: get game → get player → validate → execute via registry → record action → run handler

---
## 2026-01-25 - test-service-action-processor
- **What was implemented**: Comprehensive unit tests for ActionProcessor class (55 tests)
- **Files changed**: 
  - `app/services/ActionProcessor.test.ts` (created - 55 unit tests)
- **Learnings:**
  - Test helper `setupGameWithPlayers` creates players with specific characters (Assassin, Seer, Guardian, Fixer, Tracker) for action-specific tests
  - VoteProcessor's `checkMissionVoteCompletion` is private - test via `submitMissionVote` which returns `VoteResult` after last vote
  - Handler result is only used by ActionProcessor when `gameEnded !== undefined` - set `gameEnded: false` to use handler message without ending game
  - Test protection blocking: add status via `addStatus`, register handler that checks `hasStatus`, verify target still alive after action
  - Test action side effects (assassination, rig_vote, plant_beeper, sabotage) by registering realistic handlers that use processor's helper methods
  - `createTestAction` helper simplifies ActionDefinition creation with sensible defaults (phases, targets, validation)
  - Test both success and error paths for validation: game not found, player not found, player not in game, player dead, action not registered
  - Action handlers receive full `GameContext`, `targetIds[]`, and the `processor` instance for calling helper methods

---
## 2026-01-25 - service-state-validator
- **What was implemented**: StateValidator service for game state transition validation
- **Files changed**: 
  - `app/services/StateValidator.ts` (created)
- **Learnings:**
  - StateValidator is stateless - only needs GameService dependency for data access
  - Leader lookup uses crown_index with alive players sorted by seat_order (modulo for wraparound)
  - getMissionSize is duplicated between VoteProcessor and StateValidator - consider extracting to shared util
  - validateJoinGame returns valid:true even when player already in game (graceful handling pattern)
  - Export constants (MIN_PLAYERS, MAX_PLAYERS) for use in other services and tests
  - validateLeaveGame added as additional validation not in original spec (follows same pattern)

---
## 2026-01-25 - test-service-state-validator
- **What was implemented**: Comprehensive unit tests for StateValidator service (47 tests)
- **Files changed**: 
  - `app/services/StateValidator.test.ts` (created - 47 unit tests)
- **Learnings:**
  - `getMissionSize` helper is tested exhaustively for all player counts (5-10) and rounds (1-5)
  - When testing eliminated player validation, account for the fact that eliminating players changes the alive count which affects team size requirements - use larger initial player count (6) to maintain valid team sizes after elimination
  - Leader rotation is based on crown_index % alivePlayers.length, using sorted alive players by seat_order
  - Test both valid and invalid crown_index values including wraparound scenarios (crown_index > player count)
  - `validateJoinGame` returns valid: true for existing players (graceful duplicate handling)
  - StateValidator tests don't need complex helper factories - `setupGameWithPlayers` handles game + player creation with configurable status/phase/crownIndex

---
## 2026-01-25 - action-assassinate
- **What was implemented**: Assassinate action for the Assassin character
- **Files changed**: 
  - `app/actions/assassinate.ts` (created - action definition and handler)
- **Learnings:**
  - Actions folder created at `app/actions/` for action implementations
  - ActionDefinition includes: id, name, description, phases[], maxUses, requiresOnTeam, minTargets, maxTargets, validateTargets, execute
  - Action execute function returns ActionResult for success/failure; handler processes side effects (elimination, game end)
  - Handler receives full GameContext plus ActionProcessor instance for calling helper methods (eliminatePlayer, endGame, hasStatus)
  - Protection check can happen in both execute (return early with success=true) and handler (skip elimination)
  - Win conditions differ by phase: assassination phase wrong target = good wins; mission_voting phase doesn't trigger end game
  - Registration split: `registerAssassinateAction()` for ActionRegistry, `registerAssassinateHandler(processor)` for ActionProcessor

---
## 2026-01-25 - action-rig-vote
- **What was implemented**: Rig vote action for the Fixer character to force mission pass
- **Files changed**: 
  - `app/actions/rigVote.ts` (created - action definition and handler)
- **Learnings:**
  - Actions without targets use `minTargets: 0, maxTargets: 0` and validateTargets returns valid when targetIds.length === 0
  - Handler uses `proc.addModifier()` to create game modifier for current round
  - Modifier type `force_pass` is already handled in VoteProcessor's `processMissionResult` - sets failVotes to 0
  - Follow same pattern as assassinate: separate ActionDefinition, handler factory, and registration functions
  - No need to check requiresOnTeam for actions that work from outside the mission team

---
## 2026-01-25 - action-plant-beeper
- **What was implemented**: Plant beeper action for the Tracker character to tag players
- **Files changed**: 
  - `app/actions/plantBeeper.ts` (created - action definition and handler)
- **Learnings:**
  - Multi-target validation extracts targets first, then checks each individually for existence/alive status
  - Team validation checks both targets have `team` assigned (not null) before comparing alignments
  - Sorting teams alphabetically ['evil', 'good'] provides deterministic comparison for 1-good-1-evil requirement
  - Status expiration uses `expiresAtRound` parameter on `addStatus` - set to current round for end-of-round cleanup
  - Handler creates separate status for each target using loop over targetIds
  - Vibration trigger handled elsewhere in VoteProcessor - action just creates the 'beepered' status

---
## 2026-01-25 - action-protect
- **What was implemented**: Protect action for Guardian character to shield players from assassination
- **Files changed**: 
  - `app/actions/protect.ts` (created - action definition and handler)
- **Learnings:**
  - Protect is simpler than plant_beeper - single target, no team validation needed
  - Protection status expiration set to current round via `expiresAtRound` parameter
  - Assassinate action already checks for 'protected' status in both execute and handler - no changes needed
  - Team validation in execute confirms player is on good team (Guardian is good character)
  - Follow established pattern: ActionDefinition, handler factory, registration functions

---
## 2026-01-25 - action-sabotage
- **What was implemented**: Sabotage action for the Saboteur character to add extra fail vote
- **Files changed**: 
  - `app/actions/sabotage.ts` (created - action definition and handler)
- **Learnings:**
  - Sabotage follows same pattern as rig_vote but with `requiresOnTeam: true` - must be on mission team
  - Uses `extra_fail` modifier which is already handled by VoteProcessor.processMissionResult
  - No targets needed (minTargets: 0, maxTargets: 0) - affects the current mission vote tally
  - Team membership check in execute validates player is in selected_team array
  - VoteProcessor already increments failVotes when `extra_fail` modifier exists for current round

---
## 2026-01-25 - test-actions-all
- **What was implemented**: Comprehensive integration tests for all character actions (assassinate, rig_vote, plant_beeper, protect, sabotage)
- **Files changed**: 
  - `app/actions/actions.integration.test.ts` (created - 19 integration tests)
- **Learnings:**
  - Integration tests need fresh service instances per test - use `createTestEnv()` helper to create GameService, VoteProcessor, ActionProcessor
  - Must register both actions (ActionRegistry) and handlers (ActionProcessor) before testing
  - VoteResult uses 'passed'/'failed' strings, not 'pass'/'fail' - match actual return values
  - ActionRegistry error messages include full context: "Action X has already been used the maximum number of times (N)"
  - Test helper `setupGameWithPlayers` handles game creation, player addition with characters, and game status setup
  - Players must be added BEFORE setting game status to 'playing' (GameService.addPlayer checks status)
  - Protection blocking is tested by: protect first → assassinate → verify target still alive
  - Modifier integration tested by: add modifier → submit votes → verify result reflects modifier (force_pass, extra_fail)

---
## 2026-01-25 - util-character-assignment
- **What was implemented**: Character assignment algorithm for game start
- **Files changed**: 
  - `app/utils/characterAssignment.ts` (created)
- **Learnings:**
  - Utils folder created at `app/utils/` for game-specific utility functions
  - Team composition stored as constant lookup table (`TEAM_COUNTS`) for O(1) access
  - Fisher-Yates shuffle provides unbiased randomization - shuffle both characters and player IDs
  - `selectCharacters` ensures required character is included, then fills with shuffled optional characters
  - Return type `CharacterAssignmentResult` bundles player updates and crown_index for atomic game start
  - Player updates use `PlayerUpdate` type from game.ts for type safety
  - Crown index is 0 to N-1, randomly selected independently from seat order

---
## 2026-01-25 - test-util-character-assignment
- **What was implemented**: Comprehensive unit tests for character assignment utility (60 tests)
- **Files changed**: 
  - `app/utils/characterAssignment.test.ts` (created - 60 unit tests)
  - `app/types/game.ts` (updated - added Soldier character)
  - `app/registry/CharacterRegistry.ts` (updated - added Soldier definition)
  - `app/registry/CharacterRegistry.test.ts` (updated - added Soldier to character count tests)
- **Learnings:**
  - Tests uncovered a data inconsistency: 5 good characters defined but 6 needed for 9-10 player games
  - Added "Soldier" character (like Villager, no special abilities) to support full team compositions
  - Statistical tests for randomization use multiple iterations (30-50) and count unique results to verify variance
  - Use `it.each()` from Vitest to parameterize tests for all player counts (5-10) with expected team sizes
  - Player interface does NOT have `updated_at` field - check type definitions before creating test helpers
  - Test helpers like `createTestPlayers(count)` simplify repeated test setup
  - Tests should verify both edge cases (min/max) and all intermediate values for player count validation

---
## 2026-01-25 - api-games-create
- **What was implemented**: POST /api/games/create endpoint for creating new games
- **Files changed**: 
  - `app/routes/api.games.create.tsx` (created)
- **Learnings:**
  - React Router 7 uses `action` function exports for POST requests, not separate API files
  - File naming `api.games.create.tsx` maps to route `/api/games/create` via flat routes
  - Use `createClient(request)` from supabase server to get authenticated user via `getUser()`
  - Return `new Response(JSON.stringify(data), { status, headers })` for JSON API responses
  - Merge auth headers from supabase client with response headers to maintain session cookies
  - Type safety: Use `satisfies` operator with interface types for response bodies
  - GameService singleton (`gameService`) persists in-memory data across requests during development

---
## 2026-01-25 - api-games-join
- **What was implemented**: POST /api/games/:gameId/join endpoint for joining existing games
- **Files changed**: 
  - `app/routes/api.games.$gameId.join.tsx` (created)
- **Learnings:**
  - Dynamic route params use `$paramName` syntax in file name: `api.games.$gameId.join.tsx` → `/api/games/:gameId/join`
  - Access params via `params.gameId` in the action function's `ActionFunctionArgs`
  - StateValidator provides reusable validation logic (`validateJoinGame`) that checks game exists, is in lobby, and has space
  - GameService.addPlayer handles duplicate joins gracefully by returning existing player
  - Map error messages to status codes: 404 for not found, 400 for invalid state (full/started), 401 for unauth
  - Always validate gameId param exists before using it to avoid runtime errors

---
## 2026-01-25 - api-games-start
- **What was implemented**: POST /api/games/:gameId/start endpoint for host to start a game
- **Files changed**: 
  - `app/routes/api.games.$gameId.start.tsx` (created)
- **Learnings:**
  - StateValidator.validateGameStart handles both host check and game state validation in one call
  - Map error messages to status codes: 403 for non-host, 400 for invalid player count, 404 for not found
  - Character assignment via `assignCharacters(players)` returns both playerUpdates array and crownIndex
  - Apply player updates in a loop before updating game state to ensure atomicity
  - Game state update sets: status='playing', phase='voting_for_leader', current_round=1, crown_index from assignment
  - Response includes updated game state so client can immediately reflect changes

---
## 2026-01-25 - api-games-vote
- **What was implemented**: POST /api/games/:gameId/vote endpoint for leader and mission votes
- **Files changed**: 
  - `app/routes/api.games.$gameId.vote.tsx` (created)
- **Learnings:**
  - VoteProcessor handles all complex validation (phase check, duplicate votes, team membership, good player restrictions) - route just needs to delegate
  - Use `gameService.getPlayer(gameId, userId)` to find a player by user ID (not player ID) in a game
  - Route validates vote format before passing to processor - use type guards like `isValidVote(voteType, vote)` for type narrowing
  - Return 403 for "player not in game" (authorization error) vs 400 for "invalid vote" (bad request)
  - VoteResult type already matches API response schema - can return directly from processor

---
## 2026-01-25 - api-games-team
- **What was implemented**: POST /api/games/:gameId/team endpoint for leader team selection
- **Files changed**: 
  - `app/routes/api.games.$gameId.team.tsx` (created)
- **Learnings:**
  - StateValidator.validateTeamSelection handles all complex validation (leader check, team size, alive players) - route delegates to it
  - Game type has nullable fields (`phase: GamePhase | null`, `selected_team: string[] | null`) - response interfaces must match
  - Map validation error messages to HTTP status codes: "Only the current leader" → 403, other validation errors → 400
  - Route updates two game fields atomically: `selected_team` and `phase` (to 'mission_voting')
  - Validate request body structure (teamIds is array, all items are strings) before passing to StateValidator

---
## 2026-01-25 - api-games-action
- **What was implemented**: POST /api/games/:gameId/action endpoint for executing character special abilities
- **Files changed**: 
  - `app/routes/api.games.$gameId.action.tsx` (created)
- **Learnings:**
  - ActionProcessor.executeAction handles all complex validation (game exists, player in game, player alive, action exists, phase, use limits, targets) - route just delegates
  - Use `isActionId` type guard to validate actionId from request body against valid ActionId union type
  - Route validates request structure (actionId exists, targetIds is array of strings) before passing to processor
  - ActionResult interface from types/game.ts matches the API response schema - can return directly from processor
  - Error mapping: 403 for "player not in game" (authorization), 400 for invalid action/targets (bad request), 404 for game not found

---
## 2026-01-25 - test-api-routes
- **What was implemented**: Comprehensive integration tests for all API routes (43 tests)
- **Files changed**: 
  - `app/routes/api.routes.integration.test.ts` (created - 43 integration tests)
- **Learnings:**
  - React Router 7's `ActionFunctionArgs` requires an `unstable_pattern` property - create a helper function `createActionArgs(request, params)` to add it
  - Mock Supabase auth by using `vi.mock('~/lib/supabase/server')` with a function that checks for Authorization header
  - Use custom header (`X-Test-User-Id`) to control which user is authenticated in tests for multi-user scenarios
  - Singleton services (gameService, voteProcessor, actionProcessor) need their internal Maps cleared between tests using type casting: `(service as any).games.clear()`
  - When testing error messages, use `.toLowerCase()` or match partial strings to avoid case sensitivity issues
  - Action routes must register both actions (ActionRegistry) and handlers (ActionProcessor) before testing
  - Complete flow tests (voting rounds, win conditions) require setting up game state with specific characters, teams, and phases
  - Test authentication requirement on every route by passing `{ authenticated: false }` to createRequest helper

---
## 2026-01-25 - context-game-flow
- **What was implemented**: GameFlowContext for client-side game state management with real-time subscriptions
- **Files changed**: 
  - `app/contexts/GameFlowContext.tsx` (created)
- **Learnings:**
  - Context folder created at `app/contexts/` for React context providers
  - GameFlowContext combines state management with Supabase real-time subscriptions in one provider
  - Use `useRef` to store Supabase client and channels to prevent subscription recreation on re-renders
  - Channel cleanup via `supabase.removeChannel(channel)` in useEffect cleanup function
  - API methods wrapped in `useCallback` with `gameId` dependency to prevent unnecessary recreations
  - Support optional `initialGame` and `initialPlayers` props for SSR/loader hydration
  - Build `GameContext` (ctx) from current state: game, players, currentPlayer, modifiers (filtered by round), statuses
  - Use separate channels per table (games, players, game_actions, player_statuses) with unique names
  - Player list refetched on any player change to maintain correct seat_order sorting

---
## 2026-01-25 - test-context-game-flow
- **What was implemented**: Unit tests for GameFlowContext (31 tests)
- **Files changed**: 
  - `app/contexts/GameFlowContext.test.tsx` (created - 31 unit tests)
  - `vite.config.ts` (updated - disable React Router plugin during tests)
- **Learnings:**
  - React component tests need `@vitest-environment jsdom` annotation at top of file
  - React Router Vite plugin conflicts with jsdom tests - disable via `mode !== "test"` check in vite config
  - Mock Supabase client channels with factory function that tracks handlers and provides `_trigger()` method for simulating events
  - Import the component being tested AFTER vi.mock() calls to ensure hoisting works correctly
  - Test React contexts by creating consumer components that render context values to DOM for assertions
  - Test API methods by providing `onResult` callback to consumer components that capture return values
  - Async state update warnings (act(...) warnings) in tests are acceptable for these tests - they test the interface, not the exact timing
  - Mock fetch globally via `global.fetch = mockFetch` for API method testing

---
## 2026-01-25 - hook-game-subscription
- **What was implemented**: Standalone useGameSubscription hook for real-time game state subscriptions
- **Files changed**: 
  - `app/hooks/useGameSubscription.ts` (created)
- **Learnings:**
  - Hooks folder created at `app/hooks/` for custom React hooks
  - Hook extracts subscription logic from GameFlowContext for reusability - can be used independently or as part of larger contexts
  - Returns `{ game, players, actions, isLoading, error, refresh }` - minimal interface for game state access
  - Handles all three event types (INSERT, UPDATE, DELETE) for each table subscription
  - For INSERT events on players, refetch all players to maintain correct seat_order sorting
  - For UPDATE events, use functional setState with map to replace the specific record
  - For DELETE events, filter out the removed record by id
  - Channel names must be unique per subscription to avoid conflicts - use pattern `game-sub-${gameId}`, `game-sub-players-${gameId}`, etc.
  - Supports SSR hydration via `initialGame`, `initialPlayers`, `initialActions` options

---
## 2026-01-25 - hook-vote-subscription
- **What was implemented**: useVoteSubscription hook for real-time vote tracking
- **Files changed**: 
  - `app/hooks/useVoteSubscription.ts` (created)
  - `app/hooks/useVoteSubscription.test.tsx` (created - 31 unit tests)
- **Learnings:**
  - Vote actions stored in `game_actions` table with action_type of `vote_yes`, `vote_no`, `vote_pass`, or `vote_fail`
  - Hook filters by phase type: leader votes (yes/no) only for voting_for_leader, mission votes (pass/fail) only for mission_voting
  - `actionsToVoteMap` helper converts GameAction[] to Record<playerId, vote> with filtering by round, phase, and vote type
  - For non-voting phases (lobby, selecting_team, etc.), hook returns empty votes and skips subscriptions
  - Channel naming pattern: `vote-sub-${gameId}-${round}-${phase}` ensures unique subscription per round/phase combination
  - Real-time INSERT events must re-validate round, phase, and action type before updating local state
  - `renderHook` from @testing-library/react is cleaner for testing hooks than wrapping in consumer components
  - Supabase `.in()` query filter used to fetch only vote-related actions in one query

---
## 2026-01-25 - hook-vote-completion
- **What was implemented**: useVoteCompletion hook for monitoring vote completion and triggering server processing
- **Files changed**: 
  - `app/hooks/useVoteCompletion.ts` (created)
  - `app/hooks/useVoteCompletion.test.ts` (created - 31 unit tests)
- **Learnings:**
  - Expected voter count differs by phase: leader voting = all alive players, mission voting = selected team size
  - Use refs (`hasTriggeredRef`, `lastPhaseRef`, `lastRoundRef`) to track trigger state without causing re-renders
  - Debounce pattern uses `setTimeout` with cleanup in effect return to prevent stale triggers
  - Reset trigger state when phase OR round changes - critical for allowing new completions in new voting contexts
  - When round changes but votes remain the same (in tests), add `currentRound` to callback dependencies to force effect re-run
  - `vi.useFakeTimers()` and `vi.advanceTimersByTime()` essential for testing debounce behavior
  - Hook returns both state (`isComplete`, `voteCount`, `expectedCount`) and method (`checkCompletion`) for flexibility
  - Use `enabled` option to disable auto-triggering while still computing completion state

---
## 2026-01-25 - hook-game-api
- **What was implemented**: useGameApi hook for making game API calls with loading and error state management
- **Files changed**: 
  - `app/hooks/useGameApi.ts` (created)
  - `app/hooks/useGameApi.test.ts` (created - 31 unit tests)
- **Learnings:**
  - Hook wraps all API calls in a consistent pattern: setLoading(true) → try/catch → setLoading(false)
  - Use `withLoadingState` helper function to reduce boilerplate across all API methods
  - TypeScript closure assignments inside `act()` callbacks don't narrow types correctly - use explicit type assertions on expect statements
  - `credentials: 'include'` required on fetch calls to include auth cookies
  - Provide additional methods beyond spec (createGame, joinGame) for completeness
  - Export response types (CreateGameResponse, JoinGameResponse, etc.) for consumers to use in type annotations

---
## 2026-01-25 - page-index
- **What was implemented**: Enhanced home page with game creation and join functionality
- **Files changed**: 
  - `app/routes/__index.tsx` (updated - added auth check, game creation, join by code)
  - `app/routes/api.games.lookup.tsx` (created - game lookup by key endpoint)
- **Learnings:**
  - React Router 7 loader can check authentication via `supabase.auth.getUser()` and pass `isAuthenticated` boolean to component
  - Game code validation regex: `/^[A-Za-z0-9]{6,8}$/` for 6-8 alphanumeric characters
  - Created new `/api/games/lookup?key=XXXX` endpoint for looking up games by shareable code
  - Use `useNavigate` hook for client-side redirects after API success
  - Display name can be pre-populated from user email (split at @) for better UX
  - Loading states use inline SVG spinners for consistent animation
  - Input transformation (`.toUpperCase()`) in onChange provides immediate feedback for game codes

---
## 2026-01-25 - test-hooks-all
- **What was implemented**: Comprehensive unit tests for useGameSubscription hook (28 tests)
- **Files changed**: 
  - `app/hooks/useGameSubscription.test.tsx` (created - 28 unit tests)
- **Learnings:**
  - Mock Supabase channel with factory function tracking handlers via Map and `_trigger()` method
  - Test each table subscription independently (games, players, game_actions) - creates 3 channels total
  - For player INSERT events, hook refetches all players to maintain seat_order sorting
  - For UPDATE events, use functional setState with map to replace the specific record
  - For DELETE events, filter out the removed record by id
  - Test refresh() by using fetchCount variable to return different data on subsequent calls
  - Game type has `created_at` but no `updated_at` field - check type definitions when creating test helpers
  - Use `renderHook` from @testing-library/react for cleaner hook testing vs component wrappers
  - Note: useVibration tests are pending - hook depends on hook-vibration story which is not yet implemented

---
## 2026-01-25 - page-lobby
- **What was implemented**: Lobby page for pre-game gathering with real-time updates
- **Files changed**: 
  - `app/routes/games.$game.tsx` (rewritten - lobby page implementation)
  - `app/routes/api.games.$gameId.leave.tsx` (created - leave game endpoint)
- **Learnings:**
  - Lobby page pattern: loader fetches initial state from GameService, useEffect sets up Supabase real-time subscriptions
  - Leave endpoint must prevent host from leaving - host should transfer or delete game instead
  - Copy-to-clipboard needs fallback: try game code first, then full URL if code fails
  - Player count display: show X/10 with color coding (green for 5-10, yellow for <5)
  - Empty slots shown with dashed borders and "Waiting..." placeholder for visual clarity
  - Host indicator uses crown icon and blue styling to distinguish from regular players
  - Start button disabled state shows countdown "Need X more players" for clear feedback
  - Route params access: use `useParams()` hook, param name matches file pattern (`$game` → `params.game`)

---
## 2026-01-25 - page-game
- **What was implemented**: Main game play page container with phase routing and real-time state management
- **Files changed**: 
  - `app/routes/games.$game.play.tsx` (created - main game page)
- **Learnings:**
  - Game play page follows same pattern as lobby: loader validates auth + membership, provides initial data to GameFlowProvider
  - Use nested route pattern: `/games/:gameId` for lobby, `/games/:gameId/play` for active game
  - GameFlowProvider wraps content and provides: game, players, ctx, currentPlayer, and API methods
  - Phase routing via switch statement on `game.phase` - each phase gets its own component
  - Leader calculation: filter alive players, sort by seat_order, use crown_index % alivePlayers.length
  - Team membership check: `game.selected_team?.includes(currentPlayer.id)`
  - Evil team check for showing Fail button: `currentPlayer.team === 'evil'`
  - Placeholder components can reference useGameFlow() even if full UI is pending separate stories
  - GameOver screen reveals all player roles after game ends - iterate players with team-based styling
  - Error states: check authentication (currentUserId null), game existence, and game membership

---
## 2026-01-25 - effect-appears-as-seer
- **What was implemented**: The appears_as_seer effect for Phantom character, making it appear as a Seer candidate to the Oracle
- **Files changed**: 
  - `app/effects/appearsAsSeer.ts` (created - effect definition)
  - `app/effects/appearsAsSeer.test.ts` (created - 19 unit tests)
  - `app/effects/index.ts` (created - module exports)
- **Learnings:**
  - Effects folder created at `app/effects/` for effect implementations (parallel to actions folder)
  - EffectDefinition includes: id, name, description, hooks (empty for passive effects), modifiers array
  - Perception effects use the `modifiers` array with type `appears_as_seer` or `appears_as_good`
  - CharacterRegistry already had info resolution logic (`getSeerCandidates`) that checks for this effect - just needed registration
  - Integration tests for effects need both this effect AND `appears_as_good` registered for CharacterRegistry to work correctly
  - For tests, can inline-register effects that aren't implemented yet using `effectRegistry.register()` helper
  - Player type requires `created_at` field - check type definitions when creating test helpers

---
## 2026-01-25 - effect-appears-as-good
- **What was implemented**: The appears_as_good effect for Saboteur character, hiding them from the Seer's evil detection
- **Files changed**: 
  - `app/effects/appearsAsGood.ts` (created - effect definition)
  - `app/effects/appearsAsGood.test.ts` (created - 20 unit tests)
  - `app/effects/index.ts` (updated - added exports and registration)
- **Learnings:**
  - Follow same pattern as appears_as_seer effect: definition, test file, registration in index.ts
  - CharacterRegistry already had `getVisibleEvilPlayers()` checking for `appears_as_good` modifier - just needed effect registration
  - Saboteur already had `effects: ['appears_as_good']` in CharacterRegistry - effect was already wired up, just needed the EffectDefinition
  - Integration tests should verify both effects work together in complex scenarios (Seer + Oracle + Saboteur + Phantom)
  - Use `registerAppearsAsSeerEffect()` from the actual module in tests instead of inline registration for cleaner test setup

---
## 2026-01-25 - component-lobby
- **What was implemented**: Reusable Lobby component extracted from lobby page
- **Files changed**: 
  - `app/components/Lobby.tsx` (created - reusable lobby component)
  - `app/routes/games.$game.tsx` (updated - uses Lobby component)
- **Learnings:**
  - Extract UI components from pages for better reusability - page handles routing/data, component handles presentation
  - Component props pattern: pass game, players, currentUserId, callbacks (onStartGame, onLeaveGame), and loading/error states
  - Use `useCallback` for callback props passed to child components to prevent unnecessary re-renders
  - ARIA attributes (`aria-label`, `aria-busy`, `role="alert"`) improve accessibility for screen readers
  - Page-level concerns (loaders, navigation, real-time subscriptions) stay in the page; pure presentation logic goes in the component

---
## 2026-01-25 - component-gameboard
- **What was implemented**: GameBoard component extracted to reusable file with all sub-components
- **Files changed**: 
  - `app/components/GameBoard.tsx` (created - main GameBoard component)
  - `app/routes/games.$game.play.tsx` (updated - imports and uses GameBoard)
- **Learnings:**
  - Extract presentation components from pages for better reusability - page handles routing/data, component handles presentation
  - GameBoard accepts `renderPhase` prop to allow parent to provide phase-specific content while board handles layout
  - ScoreBoard uses round indicators with color coding: current round has blue border, past rounds show good (blue) or evil (red) based on who won
  - Progress bars visualize progress toward 3-win victory condition for each team
  - Leader calculation: filter alive players, sort by seat_order, use crown_index % alivePlayers.length
  - Export sub-components (ScoreBoard, CharacterInfoPanel, ActionPanel, PlayerList) for potential reuse elsewhere
  - Vibration listener placeholder added in useEffect - will integrate with useVibration hook when implemented
  - Phase components remain in the page file since they use useGameFlow() context methods (submitLeaderVote, selectTeam, etc.)

---
## 2026-01-25 - component-leader-voting
- **What was implemented**: Full LeaderVoting component with real-time vote tracking and results display
- **Files changed**: 
  - `app/components/LeaderVoting.tsx` (created - full voting UI component)
  - `app/routes/games.$game.play.tsx` (updated - imports and uses LeaderVoting component)
- **Learnings:**
  - Component uses useVoteSubscription for real-time vote tracking and useVoteCompletion for detecting when all votes are in
  - Double-click prevention uses ref with timestamp comparison (CLICK_DEBOUNCE_MS = 500) - cleaner than debouncing callbacks
  - Type alignment: component's `onVote` prop must match context's `submitLeaderVote` return type (`Promise<VoteResult>`, not `Promise<void>`)
  - Local state (`hasVoted`, `myVote`) provides immediate UI feedback while subscription state handles real-time updates from other players
  - Vote results are calculated client-side from the vote map when completion is detected via useVoteCompletion's `onComplete` callback
  - Reset state when `game.current_round` or `game.rejection_count` changes to handle phase transitions correctly
  - Sub-components (VotingButtons, WaitingMessage, VoteResultsDisplay) keep main component clean and improve readability

---
## 2026-01-25 - component-team-selection
- **What was implemented**: TeamSelection component for leader to select mission team members
- **Files changed**: 
  - `app/components/TeamSelection.tsx` (created - full team selection UI)
  - `app/routes/games.$game.play.tsx` (updated - imports and uses TeamSelection component)
- **Learnings:**
  - Component uses `getMissionSize` from StateValidator to determine required team size based on player count and round
  - Selection state managed locally with useState, synced to parent via `onSelectTeam` callback
  - `canSelect` prop pattern allows disabling selection during submission without unmounting component
  - Leader calculation uses same pattern as GameBoard: filter alive players, sort by seat_order, use crown_index % alivePlayers.length
  - WaitingView sub-component shows non-leaders the current selection progress with player names
  - PlayerGrid uses aria-pressed for accessibility on toggle buttons, role="group" for screen reader grouping
  - Double-click prevention via ref timestamp (CLICK_DEBOUNCE_MS) pattern, consistent with LeaderVoting component
  - Visual feedback: selected players have blue background/border, checkmark badge, and opacity changes for limit states

---
## 2026-01-25 - component-mission-voting
- **What was implemented**: Full MissionVoting component with real-time vote tracking and results display
- **Files changed**: 
  - `app/components/MissionVoting.tsx` (created - full voting UI component)
  - `app/routes/games.$game.play.tsx` (updated - imports and uses MissionVoting component)
- **Learnings:**
  - Component follows same pattern as LeaderVoting: useVoteSubscription for real-time tracking, useVoteCompletion for detecting completion
  - VoteCompletionResult from useVoteCompletion only has: voteType, voteCount, expectedCount, votes - no tally or result from server
  - Team membership check via `game.selected_team?.includes(currentPlayer.id)`
  - Good players cannot vote fail: check `isEvil` before showing/handling Fail button
  - Shuffled results display using Fisher-Yates algorithm prevents vote attribution while showing totals
  - WaitingMessage component differs from LeaderVoting - shows team member names for non-team observers
  - Reset state on `game.current_round` change only (not rejection_count like LeaderVoting) since missions don't have rejections

---
## 2026-01-25 - util-game-utils
- **What was implemented**: Centralized game utility functions for game logic
- **Files changed**: 
  - `app/utils/game-utils.ts` (created - utility functions)
  - `app/utils/game-utils.test.ts` (created - 67 unit tests)
  - `app/types/game.ts` (updated - added "Assassin failed to identify the Seer" EndReason)
- **Learnings:**
  - Some utility functions already existed in services (GameService, VoteProcessor, StateValidator) - this file consolidates and extends them
  - MISSION_SIZES constant is exported for direct access in tests and components that need to check all valid values
  - `formatJoinLink` handles browser vs server context via `typeof window !== 'undefined'` check
  - `checkWinCondition` returns `WinConditionResult` with `triggerAssassination` flag for good victory that should go to assassination first
  - Multiple helper functions for specific win conditions (`checkAssassinationFailed`, `checkAssassinationSucceeded`, `checkRejectionWin`) allow modular win checking
  - When testing Object.keys() with numeric keys, use `.sort((a, b) => a - b)` for numeric sort instead of default lexicographic sort
  - EndReason type needed to be extended to include "Assassin failed to identify the Seer" for type safety

---
## 2026-01-25 - component-assassination
- **What was implemented**: AssassinationPhase component for the assassination phase UI
- **Files changed**: 
  - `app/components/AssassinationPhase.tsx` (created - full assassination UI)
  - `app/routes/games.$game.play.tsx` (updated - imports and uses AssassinationPhase)
- **Learnings:**
  - Component follows same pattern as other voting components: props for game, players, currentPlayer, and callback (onExecuteAction)
  - Player grid uses `aria-pressed` for accessibility on toggle buttons, `role="group"` for screen reader grouping
  - Filter targetable players to exclude the Assassin themselves (can't self-target)
  - Result state stored locally - ActionResult from executeAction contains `gameEnded`, `winner`, `message` for displaying outcome
  - Waiting state shows Assassin's name dynamically by finding the Assassin player from the players list
  - Wrapper function pattern (AssassinationPhaseWrapper) in page file extracts props from useGameFlow context

---
## 2026-01-25 - component-action-panel
- **What was implemented**: Full ActionPanel component with action filtering, target selection, and execution
- **Files changed**: 
  - `app/components/ActionPanel.tsx` (created - full ActionPanel with TargetSelector)
  - `app/components/GameBoard.tsx` (updated - removed placeholder, uses new ActionPanel)
  - `app/contexts/GameFlowContext.tsx` (updated - added actions array to context value)
- **Learnings:**
  - ActionPanel uses state machine pattern (`PanelState`) for clear UI flow: idle → selecting → executing → result
  - CharacterRegistry.get(character) provides character definition with `actions` array of ActionIds
  - ActionRegistry.getAvailableActions(ctx, characterActions, usedActionIds) filters by phase and use limits
  - getUsedActionIds(playerId, gameActions) extracts special action IDs from game_actions array
  - TargetSelector is embedded inline within ActionPanel - can be extracted in component-target-selector story
  - GameFlowContext needed to expose `actions` array to track used actions - added to context interface and value
  - For actions without targets (minTargets: 0), execute immediately without showing TargetSelector
  - ResultDisplay component handles both success/error states with appropriate styling and game-end messaging
  - Click debouncing (CLICK_DEBOUNCE_MS) prevents double-submission of actions

---
## 2026-01-25 - security-server-validation
- **What was implemented**: Verified comprehensive server-side validation already exists in codebase
- **Files changed**: None (validation was already complete)
- **Learnings:**
  - Server-side validation is layered: Routes validate auth/input format → Services validate game state/business rules
  - All API routes use same pattern: check auth → validate params → delegate to service → return result
  - VoteProcessor handles: phase check, duplicate votes, team membership, good-player-fail restriction
  - ActionProcessor handles: player alive, action exists, phase correct, use limits, target validation
  - StateValidator handles: game start (host check, player count), team selection (leader check, team size, alive players)
  - Routes return 401 (unauthorized), 403 (forbidden - not in game/not leader), 404 (not found), 400 (invalid request)
  - Cannot perform actions for other players: routes use `gameService.getPlayer(gameId, user.id)` to find player from authenticated user
  - 43 integration tests in api.routes.integration.test.ts cover all security scenarios

---
## 2026-01-25 - hook-vibration
- **What was implemented**: useVibration hook for device vibration on beeper activation
- **Files changed**: 
  - `app/hooks/useVibration.ts` (created - hook and helper function)
  - `app/hooks/useVibration.test.ts` (created - 28 unit tests)
- **Learnings:**
  - Vibration API check: use `typeof navigator !== 'undefined' && 'vibrate' in navigator` for SSR compatibility
  - Supabase broadcast channels use `.on('broadcast', { event: 'name' }, handler)` for pub/sub messaging
  - Channel subscription status comes via callback to `.subscribe(status => ...)` - check for 'SUBSCRIBED' or 'CHANNEL_ERROR'
  - Graceful degradation pattern: wrap navigator.vibrate() in try/catch since some browsers may throw
  - Default vibration pattern `[200, 100, 200]` (vibrate-pause-vibrate) provides tactile feedback
  - Export `broadcastVibration` helper for server-side to trigger vibration events to clients
  - Hook returns `isSupported` boolean for UI to conditionally show vibration-related features
  - Manual `triggerVibration()` method useful for testing and user-initiated feedback
  - VibrationEvent type includes gameId, round, and targetPlayerIds for precise filtering

---
## 2026-01-25 - component-target-selector
- **What was implemented**: Extracted TargetSelector into reusable component with full validation
- **Files changed**: 
  - `app/components/TargetSelector.tsx` (created - standalone component)
  - `app/components/ActionPanel.tsx` (updated - imports TargetSelector)
- **Learnings:**
  - Extract embedded components when they have reusable value (TargetSelector used in ActionPanel, could be used in AssassinationPhase)
  - Component props pattern: pass action definition for validation info, players array, current player ID for self-targeting logic
  - Self-target actions (like 'protect') use `actionAllowsSelfTarget()` helper to determine if self-targeting is allowed
  - validateTargets delegates to action's custom validateTargets function when provided for action-specific validation
  - Use `aria-pressed` on toggle buttons and `role="group"` on container for screen reader accessibility
  - SelectionCounter sub-component handles range display (e.g., "2/2" or "1/1-3 selected") with color coding
  - atMaxAndNotSelected pattern disables unselected targets when max is reached, preventing confusion

---
## 2026-01-25 - component-character-info
- **What was implemented**: Enhanced CharacterInfoPanel to display resolved character info with effects applied
- **Files changed**: 
  - `app/components/GameBoard.tsx` (updated - enhanced CharacterInfoPanel)
- **Learnings:**
  - CharacterRegistry.resolveInfo(ctx) already handles all effect logic: Seer excludes Saboteur (appears_as_good), Oracle includes Phantom (appears_as_seer)
  - Component needs GameContext with game, players, currentPlayer, modifiers, statuses - props passed from parent that has access to useGameFlow()
  - Unreliable info detection: check if knownPlayerLabels contain "?" (e.g., "Seer?") to show warning
  - Color coding for labels: red for evil players, purple for Seer candidates, neutral for others
  - CharacterDefinition.description provides the base description, CharacterInfo.description from resolveInfo provides contextual info about what they know

---
## 2026-01-25 - component-scoreboard
- **What was implemented**: Fixed ScoreBoard round result display logic
- **Files changed**: 
  - `app/components/GameBoard.tsx` (updated - fixed round result calculation)
- **Learnings:**
  - ScoreBoard already existed with most functionality - just needed bug fix
  - Previous logic `round <= evil_victories && !isGoodWin` didn't account for good wins offsetting round numbers
  - Correct approach: past rounds that aren't good wins must be evil wins (since past = good + evil victories)
  - Game type doesn't track per-round results, only total victories - visual representation shows first N as good, rest as evil

---
## 2026-01-25 - component-player-list
- **What was implemented**: Extracted PlayerList to standalone component with selectable mode for team/target selection
- **Files changed**: 
  - `app/components/PlayerList.tsx` (created - standalone component)
  - `app/components/PlayerList.test.tsx` (created - 28 unit tests)
  - `app/components/GameBoard.tsx` (updated - imports PlayerList)
- **Learnings:**
  - Extract embedded components when they have reusable value (PlayerList used in GameBoard sidebar, could be used elsewhere)
  - Selectable mode requires: selectedIds, onSelectPlayer callback, optional canSelect filter and maxSelections
  - Default canSelect to `is_alive` check - most selection scenarios only allow selecting alive players
  - Use button element for selectable mode, div for non-selectable - better semantics and accessibility
  - `aria-pressed` attribute on buttons indicates toggle state for screen readers
  - Keyboard support: Enter and Space keys should trigger selection (via onKeyDown handler)
  - PlayerItem sub-component keeps main component clean - separate presentation logic from list management
  - Component tests in jsdom environment should use standard vitest assertions (toBeTruthy, toBeNull) not jest-dom matchers

---
## 2026-01-25 - component-vote-results
- **What was implemented**: Reusable VoteResults component with animated reveal effect and continue button
- **Files changed**: 
  - `app/components/VoteResults.tsx` (created - main component)
  - `app/components/VoteResults.test.tsx` (created - 29 unit tests)
- **Learnings:**
  - Animation phases (initial → revealing → complete) managed via useState with setTimeout transitions
  - Staggered animation via `transitionDelay` CSS style prop based on index * VOTE_STAGGER_MS
  - Auto-continue countdown uses separate useEffect that starts after phase === 'complete'
  - VoteCard sub-component uses transform + opacity transitions for reveal effect
  - shuffleArray (Fisher-Yates) used for mission votes to prevent attribution
  - Both leader (yes/no/approved) and mission (pass/fail/passed) result types supported via discriminated union props
  - @testing-library/user-event not installed - use fireEvent.click instead for click testing with fake timers

---
## 2026-01-25 - component-game-over
- **What was implemented**: GameOver component with celebration/defeat animations
- **Files changed**: 
  - `app/components/GameOver.tsx` (created - full game over screen)
  - `app/routes/games.$game.play.tsx` (updated - imports and uses GameOver)
- **Learnings:**
  - Animation patterns: use CSS keyframes for confetti/particles, inject via <style> element inside component
  - Staggered reveals use transitionDelay calculated from index * stagger constant
  - Animation phases (initial → revealing → complete) managed via useState with setTimeout transitions
  - Player prop type should accept `null` in addition to `undefined` when coming from React context (context returns `Player | null`)
  - Play Again flow: make POST to /api/games/create, extract gameId from response, navigate to new game lobby
  - Team-based animations: confetti (falling) for good, dark particles (rising) for evil - opposite directions emphasize thematic difference
  - formatEndReason helper provides user-friendly messages for all EndReason values
  - sortPlayersByTeam groups players for reveal section (good first, then evil)
  - Personal result shows if current player was on winning team - adds personal engagement

---
## 2026-01-25 - security-data-privacy
- **What was implemented**: Data privacy utilities to filter sensitive player data based on viewer's role
- **Files changed**: 
  - `app/utils/dataPrivacy.ts` (created - data filtering utilities)
  - `app/utils/dataPrivacy.test.ts` (created - 23 unit tests)
  - `app/effects/index.ts` (updated - fixed ES module imports)
- **Learnings:**
  - Data privacy layer should live in `app/utils/` as utility functions, not integrated into services
  - `filterPlayersForViewer` returns `PublicPlayer[]` with character/team set to null when hidden
  - Seer visibility excludes Saboteur via `appears_as_good` effect check through CharacterRegistry
  - Oracle sees Seer candidates including Phantom via `appears_as_seer` effect check
  - Evil team members see each other's team but not specific characters
  - Vote attribution rules: mission votes NEVER show player_id, leader votes show after all votes in
  - `getVoteTally` provides anonymous vote counts for UI display
  - `shouldRevealAllData` returns true only when game.status === 'finished'
  - Effects index.ts needed to use ES module imports instead of CommonJS require for test compatibility
  - Note: Supabase RLS policies are a separate database concern - utilities provide the filtering logic

---
## 2026-01-25 - test-security
- **What was implemented**: Comprehensive security tests for game integrity
- **Files changed**: 
  - `app/security/security.test.ts` (created - 27 tests, 5 skipped/todo)
- **Learnings:**
  - Security tests folder created at `app/security/` for security-focused test suites
  - Tests verify routes use authenticated user ID from Supabase auth, ignoring any spoofed IDs in request body
  - `gameService.getPlayer(gameId, user.id)` pattern ensures player lookup uses authenticated user ID
  - Vote/action security is layered: routes check auth → services check game membership → services check authorization
  - Phase validation happens in services (VoteProcessor, ActionProcessor) not routes - routes delegate to services
  - Rate limiting tests marked as `it.todo()` since `security-rate-limiting` story is not yet implemented
  - RLS policy tests are skipped (`it.skip`) - they require real Supabase connection, not unit testable
  - Additional authorization checks: good players can't vote fail, dead players can't act, only host can start, only leader can select team
  - Impersonation prevention: routes ignore any `userId`/`playerId`/`hostId` in request body, always use authenticated user

---
## 2026-01-25 - db-migrations
- **What was implemented**: Supabase database migrations for all game tables
- **Files changed**: 
  - `supabase/migrations/20260125000000_create_game_tables.sql` (created - 354 lines)
- **Learnings:**
  - Supabase migrations folder created at `supabase/migrations/` following standard Supabase structure
  - Migration naming convention: `YYYYMMDDHHMMSS_description.sql` for chronological ordering
  - All 6 tables created: games, players, game_actions, game_modifiers, player_statuses, game_logs
  - Use CHECK constraints for enum-like columns (status, phase, team, character, action_type, etc.)
  - JSONB type used for metadata columns (more efficient than JSON for querying)
  - UUID[] array type for selected_team and target_ids columns
  - Composite indexes match technical plan: (game_id, user_id), (game_id, round, phase), (game_id, player_id, status_type)
  - RLS enabled on all tables with policies for: SELECT, INSERT, UPDATE, DELETE
  - RLS policy pattern: check if user exists in players table for same game_id, or is game host
  - Service role bypasses RLS by default in Supabase - server-side operations have full access
  - REFERENCES with ON DELETE CASCADE ensures cleanup when parent records are deleted
  - COMMENT ON statements add documentation visible in Supabase dashboard

---
## 2026-01-25 - db-realtime-config
- **What was implemented**: Supabase real-time replication for game tables with verification tests
- **Files changed**: 
  - `supabase/migrations/20260125000001_enable_realtime.sql` (created - real-time publication config)
  - `supabase/realtime.test.ts` (created - 14 verification tests)
- **Learnings:**
  - Real-time replication uses `ALTER PUBLICATION supabase_realtime ADD TABLE tablename;` SQL command
  - Separate migration file for real-time config keeps concerns separated from table creation
  - Not all tables need real-time: game_modifiers and game_logs are server-side only, excluded from publication
  - Real-time subscriptions still respect RLS policies - clients only receive events for rows they can SELECT
  - Verification tests can parse migration file to confirm all required tables are included
  - Manual verification: `SELECT tablename FROM pg_publication_tables WHERE pubname = 'supabase_realtime';`
  - Hooks (useGameSubscription, useVoteSubscription) already set up to consume real-time events via postgres_changes
  - Migration comments should include verification queries for post-deployment checks

---
## 2026-01-25 - test-components-unit
- **What was implemented**: Comprehensive unit tests for all game components (Lobby, LeaderVoting, TeamSelection, MissionVoting, AssassinationPhase, ActionPanel, CharacterInfoPanel, ScoreBoard)
- **Files changed**: 
  - `app/components/game-components.test.tsx` (created - 45 unit tests)
- **Learnings:**
  - Component tests need `@vitest-environment jsdom` annotation at file top for browser-like environment
  - Mock hooks with vi.mock() before importing components - hooks like useVoteSubscription need complete return type including `refresh` function
  - When testing components that use useEffect to detect state from hooks, useEffect runs AFTER initial render so state changes aren't immediate
  - For testing vote-related state, prefer testing user interactions (clicking buttons, calling onVote) over testing initial state with pre-existing votes
  - Use `waitFor` and `act` from @testing-library/react for async state updates - but simpler to test actual user behavior when possible
  - When multiple elements match a query (e.g., numbers like '2'), use container.querySelector with specific CSS class selectors instead of screen.getByText
  - Regex patterns in getByText (e.g., `/Selected: 1\/2/`) are more robust than looking for exact text fragments
  - Mock getMissionSize from StateValidator to control team size requirements in TeamSelection tests
  - Test helper functions like createTestGame, createTestPlayer, createTestPlayers reduce test setup boilerplate
  - Component tests don't need to mock registries if testing presentation logic - mock the hooks that provide data to the component

---
## 2026-01-25 - ui-lobby-design
- **What was implemented**: Enhanced lobby interface with improved visual design, skeleton loading states, and consistent styling
- **Files changed**: 
  - `app/components/Lobby.tsx` (updated - added skeleton loaders, gradient avatars, improved styling)
- **Learnings:**
  - Skeleton loaders should match the layout of actual content for smooth transitions
  - Deterministic avatar colors based on player ID use simple hash: `id.split('').reduce((acc, char) => acc + char.charCodeAt(0), 0) % colors.length`
  - Tailwind gradient patterns: `bg-gradient-to-br from-{color}-500 to-{color}-700` creates depth
  - Animated pulsing indicator uses two overlapping spans: outer with `animate-ping opacity-75`, inner with solid color
  - `isLoading` prop pattern allows parent to control loading state display
  - Export both `LobbySkeleton` component (for standalone use) and internal `PlayerSkeleton` helper
  - Button active states with `active:scale-[0.98]` provide tactile feedback on click
  - Use `tabular-nums` class for numeric displays (player count) to prevent layout shifts
  - "(you)" indicator for current user improves UX by helping users identify themselves in the list

---
## 2026-01-25 - ui-game-board-design
- **What was implemented**: Enhanced main game board with improved visual design, responsive layout, and better UX
- **Files changed**: 
  - `app/components/GameBoard.tsx` (updated - comprehensive visual enhancement)
  - `app/components/game-components.test.tsx` (updated - fixed tests for new CSS classes)
- **Learnings:**
  - Phase configuration object (PHASE_CONFIGS) with icon, color, description centralizes phase-specific styling
  - Skeleton loading component (GameBoardSkeleton) should mirror actual layout structure for smooth transitions
  - `ring-offset-*` classes in Tailwind create visual depth for focus/highlight states
  - Collapsible sidebar pattern on mobile: use state toggle with conditional `hidden lg:block` classes
  - Sticky header pattern: `sticky top-0 z-10 bg-*/95 backdrop-blur-sm border-b` for semi-transparent effect
  - Progress bar animations: `transition-all duration-700 ease-out` provides smooth growth effect
  - SidebarSection component with optional title keeps sidebar structure consistent
  - Error state screens with icon + message + action button pattern improves user experience
  - When updating visual design, tests using CSS class selectors (querySelector) need updating to match new classes
  - Use attribute selectors `[class*="keyword"]` in tests for more flexible class matching
